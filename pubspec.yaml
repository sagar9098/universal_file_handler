name: universal_file_handler
description: A Flutter package to open, view, cache, and share files including image, PDF, Word, and Excel. Supports network, asset, and local files.
version: 0.1.2
license: MIT
homepage: https://github.com/sagar9098/universal_file_handler
repository: https://github.com/sagar9098/universal_file_handler
issue_tracker: https://github.com/sagar9098/universal_file_handler/issues


environment:
  sdk: ^3.8.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  path_provider: ^2.1.2
  open_filex: ^4.7.0
  share_plus: ^12.0.1
  photo_view: ^0.15.0
  flutter_pdfview: ^1.4.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:


